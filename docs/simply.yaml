project: noema-dev
version: "0.2"
phase: "03"

# Current Progress
feature: "3.3"
feature_name: "Views, Forking, and User Journeys"
task: "3.3.D1"
task_name: "Entity Layer: Schema design for entities table"

# Completed
completed_features:
  - "3.1"   # Content blocks - Content-addressed text storage with origin tracking
  - "3.1b"  # Asset storage - Binary blob storage (images, audio, PDFs)
  - "3.2"   # Conversation structure - Turns, spans, messages with content references
