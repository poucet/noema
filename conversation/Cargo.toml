[package]
name = "conversation"
version = "0.1.0"
edition = "2024"

[dependencies]
llm = { path = "../llm" }
anyhow = "1.0"
async-trait = "0.1"
futures = "0.3"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
