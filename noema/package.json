{
  "name": "noema_ui",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "cd ../simply-daemon/admin && npm run dev",
    "build": "cd ../simply-daemon/admin && npm run build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
