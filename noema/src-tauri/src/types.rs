//! Retired — the React frontend used Display-type wrappers around daemon
//! types; the shared admin UI consumes daemon types directly via ts-rs.
//! This module is empty on purpose and can be deleted once `jj` allows.
