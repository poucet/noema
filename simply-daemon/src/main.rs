//! Simply Daemon — standalone runner.
//!
//! This binary hosts the daemon as a separate process.
//! For in-process usage, depend on the `simply-daemon` library crate instead.

fn main() {
    eprintln!("simply-daemon: not yet implemented — use in-process mode via the library crate");
    std::process::exit(1);
}
