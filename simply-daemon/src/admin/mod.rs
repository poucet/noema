//! Admin dashboard — HTML page served by the axum server.
//!
//! The HTML file is embedded at compile time. The axum server serves it at `/` and `/admin`.
//! Connection tracking and kill endpoint are handled by the axum server directly.
